# 索引里一律存 LF，检出时再按下面的规则决定工作树的行尾（现状：索引已是 LF）。
* text=auto

# Shell 脚本必须始终是 LF：CRLF 会让 Linux/macOS 上的 CI 直接失败
# （./ci/run_checks.sh: bad interpreter / $'\r': command not found）
*.sh text eol=lf
*.bash text eol=lf

# Windows 批处理必须是 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做行尾转换与 diff
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary
